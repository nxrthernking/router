spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: postgres
    password: 123
    hikari:
      schema: router
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate